# Keep this value server-side. Never prefix it with NEXT_PUBLIC_.
OPENAI_API_KEY=

# Optional. MoodMix defaults to gpt-5.4-mini.
OPENAI_MODEL=gpt-5.4-mini

# Public canonical URL used for Open Graph and Twitter metadata after deployment.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
